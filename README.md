## Install Dependencies
`
    yarn install && 
    cd ios && 
    pod install
`

## RUN in Android
`
    yarn android
`

## RUN in iOS
`
    yarn ios
`

### Check email
Go to `https://mailinator.com` and type `doctor` to get the inbox